version: "3.9"
services:
  ai-multicloud-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - APP_HOST=0.0.0.0
      - APP_PORT=8000
    volumes:
      - .:/app:cached
